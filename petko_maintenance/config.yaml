name: Petko DB Maintenance
version: "1.0.0"
slug: petko_maintenance
description: Hourly cleanup for reported words and old challenge cards (Petko SR + EN)
url: https://github.com/dragangagy/petko-ha-addons
arch:
  - amd64
startup: application
boot: auto
init: false
options:
  postgres_password: homeassistant
schema:
  postgres_password: password
build:
  amd64: Dockerfile
